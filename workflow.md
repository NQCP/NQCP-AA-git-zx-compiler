# Workflow
Y/resource state generation state generation
0. get the logical blocks for a given ppr and estimate its active volume 
1. if the next ppr does not commute with the current one, need to add the bridge qubit for bell measrement 
2. Total tiles in baseline architecture are 173. 173/2 = 86.5. logical blocks available per cycle. 
calculate av for first ppr, 